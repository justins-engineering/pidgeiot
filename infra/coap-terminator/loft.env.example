# Copy to loft.env (gitignored) and fill in. This is the SAME value as
# dovecote's COAP_SERVICE_SECRET Worker secret:
#   openssl rand -base64 32                     # generate once
#   cd dovecote && bunx wrangler secret put COAP_SERVICE_SECRET   # paste it
COAP_SERVICE_SECRET=

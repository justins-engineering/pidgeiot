/target
secrets.env
# wrangler local secrets (e.g. dovecote's COAP_SERVICE_SECRET for dev)
.dev.vars
.dev.vars.*
# CoAP terminator's VPS env file (holds COAP_SERVICE_SECRET)
infra/coap-terminator/loft.env
.migration/
kratos.yml

# Claude Code session state: agent worktrees, transcripts, review artifacts.
# prune-worktrees.sh is deliberately tracked; everything else here is local.
.claude/*
!.claude/prune-worktrees.sh
